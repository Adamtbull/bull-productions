# API reconstruction area

Do not implement production replacement handlers until each route has been reconstructed and tested against the current live behaviour.

The original Vercel serverless source is not available through the current connector. This folder intentionally begins as documentation rather than fake/stub production code.
